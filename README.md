# DE_delay
библиотека для работы со временем

подключение:
```cpp
#include <DE_delay.h>
```
объекты:
```cpp
Delays Delay;
```
методы:
```cpp
Delay.Micr(uint16_t);  //миллисекунды
Delay.Mill(uint32_t);  //микросекунды
```
